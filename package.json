{
  "name": "voucher-seat-assignment",
  "version": "1.0.0",
  "private": true,
  "description": "Airline Promotional Voucher Seat Assignment — pnpm monorepo",
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
